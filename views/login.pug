doctype html
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
    title Login
    link(rel="stylesheet" href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css")
  body(class="bg-gray-100")
    .flex.items-center.justify-center.h-screen
      .w-full.max-w-md.bg-white.shadow-lg.rounded-lg.p-8
        h1.text-2xl.font-bold.text-center.mb-6 Login
        
        form(action="/admin" method="POST")
          .mb-4
            label(for="email" class="block text-gray-700") Email:
            input(type="email" name="email" id="email" class="w-full px-4 py-2 border rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500" placeholder="Enter your email" required)

          .mb-4
            label(for="password" class="block text-gray-700") Password:
            input(type="password" name="password" id="password" class="w-full px-4 py-2 border rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500" placeholder="Enter your password" required)

          .flex.items-center.justify-between.mb-4
            label(for="remember" class="inline-flex items-center text-gray-600")
              input(type="checkbox" id="remember" name="remember" class="form-checkbox h-4 w-4 text-blue-600")
              span.ml-2 Remember me

          button(type="submit" class="w-full bg-blue-500 text-white px-4 py-2 rounded-md hover:bg-blue-600") Login
        
        .text-center.mt-4
          a(href="/admin/register" class="text-blue-500 hover:underline") Don't have an account? Register

